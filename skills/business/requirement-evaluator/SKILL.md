---
name: requirement-evaluator
description: |-
  需求满足度与二开成本评估 Skill。基于 `客户需求解析 + 产品功能清单匹配 + 二开复杂度分级 + 成本估算` 方案，
  把一份客户需求书评估成"产品满足多少、差多少、二开要多少人天多少钱、能不能打这个单"。
  触发场景："评估一下这个需求我们满足多少"、"MI能满足这个客户吗"、"帮我算算二开量和成本"、
  "这个标能不能打"、"需求差距分析"、"功能覆盖率评估"。
  仅面向售前打单的需求评估与应标决策，不负责写方案/报价单/投标文件（那些交给后续兄弟 Skill）。

  不做的事情：
  - 不写立项建议书（交给 project-proposal-generator）
  - 不写公司介绍/方案汇报（交给 company-intro-generator）
  - 不写投标文件（交给 bid-doc-master）
  - 不生成产品 PRD（交给 product-prd-generator）
  - 不做需求结构化入库（交给 material-importer）
compatibility: >
  依赖素材库 $MATERIALS_DIR（通过 LANLNK_BASE 环境变量配置）。
  依赖 markitdown 转化需求文档（docx/pdf → md）。
  依赖产品功能清单（product-prd-generator 产物，位于 prd/<产品>/output/功能清单.md）。
  纯提示词 skill，无 Python 依赖。

  Quick start:
  ```bash
  export LANLNK_BASE=/opt/code/docs/lanlnk
  ```

  用户只需告诉 Agent 客户需求文档路径和评估基准产品，Agent 自动完成：
  - "评估一下 incoming/需求-XX招商管理.docx，看 MI 满足多少"
  - "这份需求书，商管系统能覆盖吗，差多少，二开多少钱"
  - "客户给了需求，帮我算算能不能打这个单"
---

# Requirement Evaluator — 需求满足度与二开成本评估 Agent Pipeline

基于 `需求解析 + 产品功能匹配 + 二开复杂度分级 + 成本估算 + 应标建议` 的方案。
用户说"评估一下这个需求"，Agent 负责转化需求、匹配产品、识别二开、算成本、给应标建议。

---

## 配置读取

设置 `LANLNK_BASE` 环境变量指向素材根目录（默认当前目录），然后设置路径变量：

| 变量 | 路径 | 说明 |
|------|------|------|
| `$MATERIALS_DIR` | `$LANLNK_BASE/materials` | 素材库根 |
| `$PROPOSALS_DIR` | `$MATERIALS_DIR/14-proposals` | 评估报告输出位置 |
| `$INCOMING_DIR` | `$LANLNK_BASE/incoming` | 需求文档暂存区 |
| `$PRD_DIR` | `$LANLNK_BASE/prd` | 产品 PRD 套件根（功能清单来源）|

### 产品功能清单定位

评估基准产品的功能清单由 product-prd-generator 生成，位于 `$PRD_DIR/<产品>/output/功能清单.md`。

| 产品代号 | 功能清单路径 | 说明 |
|---|---|---|
| MI / 商管系统 | `$PRD_DIR/商管系统/output/功能清单.md` | 345 项功能，含 existing/missing 状态 |
| CRM / 会员系统 | `$MATERIALS_DIR/03-products/CRM会员系统功能清单.md` | 会员 CRM 功能 |
| AI Skills | `$MATERIALS_DIR/11-cre-ai-skills/02_机会与产品/岗位 AI Skills 增强性与摩擦消除分析矩阵.md` | AI 岗位 Skill 能力 |

> 若功能清单不存在，Agent 应提示用户先运行 product-prd-generator 生成，或由用户提供功能清单路径。

---

## 业务流程

```
客户需求文档（docx/pdf/md）+ 评估基准产品
        ↓
P0: 输入解析 → 转化需求文档 + 定位功能清单
        ↓
P1: 需求结构化 → 把需求书拆成可评估的原子条目
        ↓
P2: 产品能力匹配 → 逐条对照功能清单，标注满足度
        ↓
P3: 二开项识别 → 从差距项提取二开清单 + 复杂度分级
        ↓
P4: 二开成本估算 → 人天 × 单价 + 分期归属
        ↓
P5: 输出评估报告 → 满足度 + 二开清单 + 成本 + 应标建议
```

---

## P0: 输入解析

### 0.1 从用户描述中提取关键参数

| 参数 | 说明 | 示例 |
|------|------|------|
| 需求文档路径 | 客户需求书的文件路径 | `incoming/需求-XX招商管理.docx` |
| 评估基准产品 | 评估对照哪个产品 | MI（商管系统）/ CRM（会员）/ AI Skills |
| 客户名（可选）| 用于输出目录命名 | XX资产公司 |
| 单价基准（可选）| 二开人天单价，默认 1500 元/天 | 1500 / 2000 |
| 客户业态（可选）| 辅助理解需求 | 写字楼出租 / 购物中心 / 产业园区 |

### 0.2 需求文档转化

若需求文档为 docx/pdf，先用 markitdown 转化为 md：

```bash
markitdown "<需求文档>" -o "<同目录>/<同名>.md"
```

转化后的 md 作为评估的权威输入。

### 0.3 定位产品功能清单

根据评估基准产品，定位 `$PRD_DIR/<产品>/output/功能清单.md`。

- 功能清单包含功能名、状态（existing/partial/missing）、置信度
- `existing` = 产品已有该功能
- `partial` = 部分实现
- `missing` = 未实现

若功能清单不存在，提示用户：
```
未找到 <产品> 的功能清单。请先运行 product-prd-generator 生成，或提供功能清单路径。
```

### 0.4 信息不完全的处理

用户可能只说"评估一下这个需求"，此时 Agent 应主动追问：

```
✅ 必须确认：
  - 需求文档路径（评估对象）
  - 评估基准产品（对照哪个产品）

⚠️ 建议确认：
  - 客户名（影响输出目录）
  - 客户业态（辅助理解需求语义）
```

---

## P1: 需求结构化

### 1.1 把需求书拆成可评估的原子条目

读转化后的需求 md，按需求书的章节结构，拆成**可独立评估的原子需求条目**。

**拆分原则**：
- 保留需求书的原始章节编号（一一对应可追溯）
- 每个条目是一个可评估的功能点（太笼统的拆开，太细的合并）
- 每条提取：需求描述 + 所属模块 + 优先级推断

### 1.2 原子条目结构

```markdown
| 编号 | 需求条目 | 所属模块 | 优先级推断 |
|---|---|---|---|
| 5.1.1 | 写字楼房源建档（房号/面积/产权/状态）| 房源资产管理 | 必需 |
| 5.1.2 | 商业标准商铺（编号/面积/业态/筛选）| 房源资产管理 | 必需 |
```

**优先级推断依据**：
- **必需**：需求书明确"必须/核心/本期建设"
- **期望**：需求书描述为建设目标但未强调必需
- **可选**：需求书提及但属扩展/远期

---

## P2: 产品能力匹配

### 2.1 逐条对照功能清单

对 P1 的每个原子条目，在产品功能清单中查找对应功能，标注满足度：

| 满足度标记 | 含义 | 判定依据 |
|---|---|---|
| ✅ **完全满足** | 产品直接覆盖 | 功能清单 existing 且语义匹配 |
| ⚠️ **部分满足** | 有相关功能但需配置/适配 | 功能清单 existing 但需调整，或 partial |
| 🔨 **需二开** | 产品缺失，需开发 | 功能清单 missing |
| ❌ **不满足** | 非产品范围 | 需第三方/客户自建/非本产品定位 |

### 2.2 匹配输出表

```markdown
| 需求编号 | 需求条目 | 满足度 | 对应产品功能 | 说明 |
|---|---|---|---|---|
| 5.1.1 | 写字楼房源建档 | ✅ | 铺位管理、空间结构管理、楼层管理 | 直接覆盖 |
| 5.1.2 | 写字楼售卖（产权交易）| 🔨 | —（无房屋买卖）| MI 无售卖，需二开 |
```

### 2.3 匹配原则

- **宁可标注 ⚠️ 不标注 ✅**：功能清单有相关功能但语义不完全匹配时，标 ⚠️ 并说明差距
- **关联证据**：✅/⚠️ 必须引用功能清单里的具体功能名，不能泛泛说"有"
- **诚实标注 🔨**：功能清单 missing 或无对应功能时，诚实标 🔨，不要牵强匹配

---

## P3: 二开项识别 + 复杂度分级

### 3.1 从差距项提取二开清单

从 P2 的 🔨（需二开）和部分 ⚠️（需适配开发）项中，提取二开清单。

```markdown
| # | 二开项 | 需求来源 | 复杂度 | 说明 |
|---|---|---|---|---|
| 1 | 写字楼房屋买卖合同/产权交易 | 5.1.2 / 5.3 | L | 新业务线，含合同/房款/认购 |
| 2 | 销售数据 API 上报（对接 POS）| 5.6.2 | M | 接口对接 + 数据校验 |
```

### 3.2 复杂度分级标准

| 复杂度 | 特征 | 人天基准 | 典型场景 |
|---|---|---|---|
| **S 简单** | 配置项/规则/报表/字段扩展 | 2-5 天 | 加一个计费规则、一个审批流字段、一个报表列 |
| **M 中等** | 单模块新功能/接口对接/页面开发 | 8-15 天 | API 对接（POS/停车/财务）、佣金确权规则、商户画像分级 |
| **L 复杂** | 跨模块/新业务线/核心架构改动 | 20-40 天 | 房屋买卖全流程、活动ROI归因引擎、热力图可视化系统 |

**分级依据**：
- 是否跨模块（单模块 = S/M，跨模块 = L）
- 是否新业务线（新业务线 = L）
- 是否需要接口对接（对接 = M 起）
- 是否核心架构改动（架构改动 = L）

### 3.3 人天基准可调整

人天基准是默认参考值。skill 运行时若用户提供历史项目数据或自定义基准，优先使用用户值。

---

## P4: 二开成本估算

### 4.1 成本计算公式

```
单二开项成本 = 复杂度人天基准 × 单价
  - S: 取基准中值 3 天
  - M: 取基准中值 11 天
  - L: 取基准中值 30 天

总二开工作量 = Σ 各项人天
总二开成本 = Σ 各项成本 = 总人天 × 单价

应标报价建议 = 总二开成本 × 利润系数(1.3~1.8)
  - 满足度高(✅>70%)：利润系数 1.5~1.8（产品价值主导）
  - 满足度中(✅40-70%)：利润系数 1.3~1.5（二开主导，竞争报价）
  - 满足度低(✅<40%)：不建议主力应标，或重新定位
```

### 4.2 单价基准

| 默认 | 说明 |
|---|---|
| 1500 元/人天 | 蓝联标准开发人天单价（可在运行时由用户覆盖）|

### 4.3 分期归属

每个二开项标注建议分期：

| 分期 | 含义 | 判定 |
|---|---|---|
| **P0** | 首期必须 | 客户必需 + 经营闭环核心 |
| **P1** | 二期推进 | 客户期望 + 增强体验 |
| **P2** | 三期深化 | 可选 + 深度分析 |
| **P3** | 远期规划 | PRD 已规划该阶段 |

### 4.4 成本汇总表

```markdown
| 分期 | 二开项数 | 总人天 | 总成本(万元) |
|---|---|---|---|
| P0 | 3 | 45 | 6.75 |
| P1 | 2 | 22 | 3.30 |
| P2 | 1 | 15 | 2.25 |
| **合计** | **6** | **82** | **12.30** |
```

---

## P5: 输出评估报告

### 5.1 输出目录

```
$PROPOSALS_DIR/<客户名>/
├── 需求评估_<产品>_<YYYYMMDD>.md      ← 主报告
└── （后续方案/报价/投标文件由兄弟 Skill 生成）
```

**目录规则**：
- 客户名从需求文档标题或用户输入提取，做目录名规整（去空格/特殊字符）
- 客户目录不存在则创建
- 评估报告是该客户打单目录的**第一份文件**

**降级规则**：
- 若用户未提供客户名，报告输出到 `$INCOMING_DIR/需求评估_<产品>_<YYYYMMDD>.md`
- 并提示："后续确认客户名后，建议将报告迁入 `$PROPOSALS_DIR/<客户名>/`"

### 5.2 报告结构

```markdown
# 需求评估：<客户> — <产品>满足情况

> 评估对象：《<需求书名>》
> 评估基准：<产品>（功能清单 <路径>，<N> 项功能）
> 评估日期：YYYY-MM-DD
> 客户业态：<业态>

## 〇、总体结论
- 满足度分布：✅ X%（N项）/ ⚠️ X%（N项）/ 🔨 X%（N项）/ ❌ X%（N项）
- 二开总工作量：X 人天
- 二开总成本估算：X 万元（单价 Y 元/天）
- 应标报价建议区间：X~X 万元
- **应标建议**：✅可主力应标 / ⚠️需谨慎（说明）/ ❌不建议

## 一、逐条评估（按需求书章节一一对应）
### 模块1：XXX
| 需求编号 | 需求条目 | 满足度 | 对应功能 | 二开复杂度 | 人天 | 分期 |
（每章一表）
### 模块2：XXX
...

## 二、二开清单
| # | 二开项 | 需求来源 | 复杂度 | 人天 | 成本(万) | 优先级 | 分期 |

## 三、成本汇总
| 分期 | 二开项数 | 总人天 | 总成本(万) |
| P0/P1/P2/P3 + 合计 |

## 四、应标建议
### 能打/不能打的理由
### 分期建议（P0 首期范围 / P1+ 后续）
### 需客户确认的关键问题清单（3-5 个）

## 附录：评估依据
- 需求文档转化稿路径
- 产品功能清单路径
- 人天基准/单价说明
```

### 5.3 应标建议判定逻辑

| 满足度 | 二开占比 | 建议 |
|---|---|---|
| ✅ > 70% | < 15% | ✅ **可主力应标**，产品价值主导 |
| ✅ 40-70% | 15-35% | ⚠️ **可应标但需谨慎**，二开成本需客户接受 |
| ✅ < 40% | > 35% | ❌ **不建议主力应标**，或重新定位（如改卖服务/集成）|

---

## 典型使用流程（对话示例）

### 示例 1：标准评估

> 用户："评估一下 incoming/需求-XX招商管理.docx，看 MI 满足多少，二开多少"
>
> Agent：
> ```
> 我帮你评估。先确认：
> 1. 评估基准产品是 MI（商管系统）吗？
> 2. 客户名是什么？（用于输出目录）
> ```
>
> 用户："对，MI。客户是XX资产公司"
>
> Agent → markitdown 转化需求 → 读 MI 功能清单 → 逐条匹配 → 识别二开 → 算成本 → 输出报告到 `14-proposals/XX资产公司/需求评估_MI_20260705.md`

### 示例 2：快速判断

> 用户："这个标我们能不能打？需求在 incoming/XX需求.docx"
>
> Agent：
> ```
> 需要先确认评估基准产品（MI/CRM/AI Skills？）和客户名。
> ```

### 示例 3：自定义单价

> 用户："评估这个需求，二开单价按 2000 元/天算"
>
> Agent → 使用 2000 元/天单价计算成本

### 示例 4：明确拒绝串台

> 用户："顺便给我做个方案汇报"
>
> Agent：
> ```
> 我这次只做需求评估。
> 方案汇报请交给 company-intro-generator。
> 评估报告完成后可直接复用给后续 Skill。
> ```

---

## 与兄弟 Skill 的关系

```
material-importer（素材入库）
    ↓
requirement-evaluator（需求评估）← 你在这里
    ↓ 评估通过，决定打单
project-proposal-generator（立项报告）
    ↓
company-intro-generator（方案汇报/公司介绍）
    ↓
bid-doc-master（投标文件）
```

**横向依赖**：
- **product-prd-generator** → 产出产品功能清单（本 Skill 的评估基准）
- 本 Skill 产出的评估报告 → 可被 project-proposal-generator / company-intro-generator 复用

**复用关系**：
- 评估报告里的"满足度分析"可复用到方案汇报的"需求理解"章节
- 评估报告里的"二开清单"可复用到报价单
- 评估报告里的"需客户确认问题"可复用到售前沟通清单

---

## 不做的事情（边界）

| 不做 | 交给谁 |
|---|---|
| 写立项建议书 | project-proposal-generator |
| 写方案汇报/公司介绍 | company-intro-generator |
| 写投标文件 | bid-doc-master |
| 生成产品 PRD | product-prd-generator |
| 需求文档入库归档 | material-importer |
| 精确到代码行的二开评估 | 开发团队（本 Skill 只做产品级估算）|

---

## 素材缺口提示

评估过程中若发现基准不足，主动提示：

| 场景 | 提示内容 |
|------|---------|
| 无产品功能清单 | "未找到 <产品> 的功能清单。请先运行 product-prd-generator 生成，或提供功能清单路径。" |
| 需求文档质量差 | "需求文档描述过于笼统，已做合理拆分，建议与客户确认需求细节后再定稿评估。" |
| 功能清单过时 | "功能清单生成日期较早，可能与当前产品状态有偏差，建议重新生成。" |
| 二开项识别不充分 | "部分需求项语义模糊，二开评估可能偏低，建议与开发团队复核。" |
